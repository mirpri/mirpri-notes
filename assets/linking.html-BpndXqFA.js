import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as r,a as d,o}from"./app-7d9gukLJ.js";const a={};function n(s,t){return o(),r("div",null,[...t[0]||(t[0]=[d('<h1 id="linking" tabindex="-1"><a class="header-anchor" href="#linking"><span>Linking</span></a></h1><h2 id="预处理、编译、汇编" tabindex="-1"><a class="header-anchor" href="#预处理、编译、汇编"><span>预处理、编译、汇编</span></a></h2><p><strong>预处理</strong>：<code>gcc -E</code> 编译器处理预处理命令，包括对头文件的包含，宏定义的展开，条件编译的选择等<br><strong>编译</strong>：<code>gcc -S</code> 进行词法分析、语法分析、语义分析，进行代码优化和存储分配，生成<strong>汇编语言程序</strong><br><strong>汇编</strong>：<code>gcc -c</code> 将汇编语言代码转化为机器语言代码，生成<strong>可重定位目标文件</strong></p><h2 id="可重定位目标文件" tabindex="-1"><a class="header-anchor" href="#可重定位目标文件"><span>可重定位目标文件</span></a></h2><h3 id="链接视图-elf格式" tabindex="-1"><a class="header-anchor" href="#链接视图-elf格式"><span>链接视图（ELF格式）</span></a></h3><p>“节”是 ELF 文件中具有相同特征的最小可处理单位。</p><table><thead><tr><th>节名</th><th>内容</th></tr></thead><tbody><tr><td><strong><code>.text</code></strong></td><td>机器指令（CPU 执行的二进制代码）。</td></tr><tr><td><strong><code>.rodata</code></strong></td><td>只读数据（如 <code>printf</code> 里的字符串字面量、<code>const</code> 常量）。</td></tr><tr><td><strong><code>.data</code></strong></td><td><strong>已初始化且不为 0 的全局变量和静态(<code>static</code>)变量</strong>。</td></tr><tr><td><strong><code>.bss</code></strong></td><td><strong>未初始化或初始值为 0 的全局变量和静态(<code>static</code>)变量</strong>（在文件中不占空间，只占一个大小标记）。</td></tr><tr><td><strong><code>.symtab</code></strong></td><td>符号表（记录了程序里所有全局变量、函数的名字和它们的地址映射）。</td></tr><tr><td><strong><code>.strtab</code></strong></td><td>字符串表（存的是各个符号名字的纯文本字符串）。</td></tr><tr><td><strong><code>.rela.text</code></strong></td><td>代码重定位表（<code>.text</code> 中需要重定位的条目）。</td></tr><tr><td><strong><code>.rela.data</code></strong></td><td>数据重定位表，告诉链接器，<code>.data</code> 里哪些全局数据指针需要修正</td></tr><tr><td><strong><code>节头表</code></strong></td><td>描述每个节的基本信息，包括在文件里的偏移量、大小、访问属性、对齐方式等。</td></tr><tr><td><strong><code>ELF头</code></strong></td><td>目标文件基础信息，介绍这个文件是 32位还是64位、魔数以及程序执行的入口地址。</td></tr></tbody></table><h3 id="执行视图" tabindex="-1"><a class="header-anchor" href="#执行视图"><span>执行视图</span></a></h3><p>“段”是具有某种共同的性质的节的集合</p><ul><li>程序头表：描述可执行目标文件中的段与虚拟地址空间的映射关系，每个表项记录一个段的映射等相关信息</li><li>数据段/可读可写段：.data节和.bss节</li><li>代码段/只读代码段：.text和.rodata等</li></ul><p>相对于链接视图，多一个程序头表（段头表），用于说明可执行目标文件的段的组成等；并且多一个.init节，其中定义了一个_init函数，用于可执行目标文件开始执行时的初始化工作；此外少了两个用于重定位的信息节(即.rel.text节和.rel.data节)；</p><h2 id="链接" tabindex="-1"><a class="header-anchor" href="#链接"><span>链接</span></a></h2><p>将多个可重定位的目标文件合成一个可执行文件。</p><h3 id="符号解析" tabindex="-1"><a class="header-anchor" href="#符号解析"><span>符号解析</span></a></h3><p>将程序中每一个符号引用与一个确定的符号定义建立起唯一的关联</p><p>链接器会扫描所有输入的模块和库文件，按照<em>强弱符号</em>的规则解决多重定义问题，确保每个被引用的符号都能找到唯一确定的存储空间</p><ul><li><p>全局符号：本模块内部定义并被其他模块引用的符号</p></li><li><p>本地符号：本模块内部定义并仅能由本模块内引用的符号</p></li><li><p>外部符号：由其他模块定义并被本模块引用的符号</p></li><li><p>强符号：函数名和已初始化的全局变量</p></li><li><p>弱符号：未初始化的全局变量名</p></li></ul><div class="hint-container note"><p class="hint-container-title">Note</p><p>外部符号和本地符号无强弱之分，若出现重复定义，在编译阶段就能检查到</p></div><p>多重定义符号处理规则：</p><ol><li>强符号不能多次定义</li><li>一个符号被说明为一次强符号和多次弱符号定义，以强符号定义为准</li><li>若有多个弱符号定义，则任选其中一个</li></ol><h3 id="重定位" tabindex="-1"><a class="header-anchor" href="#重定位"><span>重定位</span></a></h3><p>在合并生成执行文件时，重新确定每条指令的地址、每个数据的地址、在指令中确定所引用符号对应的地址。</p><ul><li>合并相同的节</li><li>确定合并后符号的最终地址</li><li>修改引用地址，填入真实地址</li></ul>',23)])])}const l=e(a,[["render",n]]),p=JSON.parse('{"path":"/Computer-Systems/linking.html","title":"Linking","lang":"en-US","frontmatter":{"order":5},"git":{"createdTime":1779892474000,"updatedTime":1779892474000,"contributors":[{"name":"Mirpri","username":"Mirpri","email":"71537369+Mirpri@users.noreply.github.com","commits":1,"url":"https://github.com/Mirpri"}]},"readingTime":{"minutes":3.25,"words":976},"filePathRelative":"Computer-Systems/linking.md"}');export{l as comp,p as data};
